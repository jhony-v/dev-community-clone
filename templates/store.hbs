import { createStore } from "@harlem/core";

export interface I{{Name}}State {}

const {{name}}InitialState: I{{Name}}State  = {};

const {{name}}Store = createStore("{{name}}Store", {
  post: {{name}}InitialState
});

